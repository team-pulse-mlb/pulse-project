# API 기본 주소.
# 로컬 개발은 Vite 프록시(/api → localhost:8080)를 쓰므로 비워 두면 된다.
# 배포(S3+CloudFront) 빌드에서만 API 도메인을 지정한다. 프론트가 HTTPS이므로 API도 HTTPS여야 한다(혼합 콘텐츠 차단).
# 예: VITE_API_BASE_URL=https://api.pulsemlb.com
VITE_API_BASE_URL=
