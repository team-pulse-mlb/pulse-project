# 스키마는 prod 프로파일 Flyway로 이미 적용된 상태에서 이전 배치만 수행한다.
spring:
  main:
    web-application-type: none
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

pulse:
  game-processor:
    # 마이그레이션 배치가 운영 score.tasks 큐를 소비하지 않도록 game processor 역할을 비활성화한다.
    enabled: false
